#include<iostream>//基礎庫
#include<vector>//動態陣列
#include<tuple>//元組
#include<set>//set
#include<algorithm> //演算法庫
#include<sstream>//字串處理
#include<string>//字串
#include<bits/stdc++.h>//萬用庫(基本上來說)
using namespace std;
int main(){
    /*
    const:常數化  (const auto &entry : results)  &引用

    auto:自動格式

    sort(陣列.begin(),陣列.end(),[](匿名函式傳入值)){匿名函式內容}

    for(auto i:陣列){}:歷遍陣列法

    vector <格式> 陣列:自動陣列  <pair<int,int>>對組({,})  <tuple<格式群>>元組  <>

    make_tuple() 

    ios::sync_with_stdio(false);:取消c&cpp連動

    cin.tie(nullptr);:取消緩衝

    get<n> 獲取元組中特定n格之值

    .push_back(num) py>>.append()

    stoll stoi 字串改數字(long long,int)

    // 轉成字串並補零
        string s = to_string(num);
        while (s.size() < d) s = "0" + s;  // 確保是 d 位數

    !seen.insert(num).second:插入檢查

    
    
    */
    cout<<"1"<<endl;
    return 0;
}